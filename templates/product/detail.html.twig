{% extends 'base.html.twig' %}

{% block title %}{{ product.name}}- Village Green{% endblock %}

{% block content %}
    <h1>{{ product.name }}</h1>
    <div class="row product-detail-container my-5">
        <div class="col-md-4">
            <img src="/assets/uploads/{{ product.illustration }}" alt="{{ product.name }}">
        </div>
        <div class="col-md-8 my-auto">
            <p>
                {{ product.description }}
            </p>
            <p><b>{{ (product.price/100)|number_format(2, '.', ',') }} €</b></p>
            <a href="#" class="btn btn-success btn-block">Ajouter au panier</a>
        </div>
    </div>

{% endblock %}
